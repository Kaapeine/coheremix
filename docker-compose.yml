services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: coheremix
      POSTGRES_PASSWORD: coheremix
      POSTGRES_DB: coheremix
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
